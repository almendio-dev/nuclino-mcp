# Nuclino API Key (Required)
# Can be obtained from your Nuclino profile page: https://app.nuclino.com/users/me
# This key is used to authenticate with the Nuclino API
NUCLINO_API_KEY=your-nuclino-api-key-here

# MCP Server Authentication Key (Optional)
# If set, clients must provide this key via the X-MCP-Auth-Key header when sending messages
# Leave commented out to disable MCP server authentication
# MCP_AUTH_KEY=your-secret-mcp-auth-key

# Transport Type
# Options: 'http' or 'stdio'
TRANSPORT_TYPE=http

# HTTP Server Port (only used for HTTP transport)
PORT=3000
